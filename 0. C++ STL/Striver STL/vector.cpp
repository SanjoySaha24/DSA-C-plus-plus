#include<bits/stdc++.h>
using namespace std;

int main(){
     vector <int>v;
    // v.push_back(1);
    // cout<<v.capacity();
    // v.push_back(2);
    // cout<<v.capacity();

    // vector<pair<int,int>>vec;
    // v.emplace_back(1,2);

    vector<int> v(5,100);
    
}