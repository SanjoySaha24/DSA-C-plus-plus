#include<bits/stdc++.h>
using namespace std;

int main(){
    int arr[5];
    cin >> arr[0] >> arr[1] >>arr[2] >>arr[3] >>arr[4];
    arr[3] = arr[3]+10;
        cout<<arr[3];
    return 0;
}

// datatype should be same in arrays
// indices starts from 0
// Ex : Indices ~ 0 1 2 3 4
//         SIZE ~ 5
// array elements are stored in consecutive memory address